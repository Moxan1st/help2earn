# Help2Earn

基于 SpoonOS 的 DePIN（去中心化物理基建）网络。利用 Gemini 多模态能力作为验证节点，激励全球用户通过「拍照赚钱」模式，构建世界上最精准的无障碍数据层。

## Live Demo

| 服务 | 地址 |
|------|------|
| **Frontend** | https://help2earn-tau.vercel.app/ |
| **Backend API** | https://help2earn-api.onrender.com |

## Smart Contracts (Sepolia Testnet)

| 合约 | 地址 |
|------|------|
| Help2EarnToken | [`0x491c88aBE3FE07dFD13e379dE44D427bA94CE4C9`](https://sepolia.etherscan.io/address/0x491c88aBE3FE07dFD13e379dE44D427bA94CE4C9) |
| RewardDistributor | [`0xDf929aD0C7f32B9E3cce6B86dEaD2ff1522EF0A4`](https://sepolia.etherscan.io/address/0xDf929aD0C7f32B9E3cce6B86dEaD2ff1522EF0A4) |

---

## 核心场景

### 场景 A：数据矿工 (The Miner)

**人物**：Kenji，深圳南山的大学生

**动作**：Kenji 在去学校的路上，发现一家新开的咖啡馆门口有完美的轮椅坡道。他拿出手机，打开 Help2Earn，拍了一张门口的照片并上传。

**结果**：3 秒后，AI 验证通过，Kenji 的钱包收到了 50 个 tokens 代币（够喝个奶茶）。

**价值**：零工经济，人人都能参与。

### 场景 B：数据消费者 (The User)

**人物**：Sato 先生，轮椅使用者

**动作**：Sato 想去这家咖啡馆，但在 Google Maps 上只看到模糊的标签。他打开 Help2Earn。

**结果**：他不仅看到了「有坡道」的标记，还看到了 AI 对坡道的分析：「坡度小于 5 度，路面平整，但注意：自动门开启时间较短。」

**价值**：真正的出行安全感。

---

## 技术架构

```
┌─────────────────────────────────────────────────────────────────┐
│  Input Layer (输入层)                                            │
│  用户前端 (Web/Mobile) 上传图片 + GPS 坐标                         │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│  Processing Layer (SpoonOS Agent 层)                             │
│  ├── Vision Analysis: 识别图片内容（坡道/楼梯/其他）                 │
│  └── Logic Check: 验证有效性，提取元数据                           │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│  Settlement Layer (结算层)                                       │
│  AI 返回 {valid: true} → Agent 触发智能合约 → 向用户转账 Token      │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│  Storage Layer (数据层)                                          │
│  结构化数据存入数据库，供前端地图调用                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 功能：看 + 帮

### 看

UI 背景显示用户当前半径 200m 的地图，标出所有无障碍设施：

| 图标 | 设施类型 |
|------|---------|
| 🛤️ | 坡道 |
| 🚻 | 无障碍厕所 |
| 🛗 | 升降电梯 |
| ♿ | 轮椅借用处 |
| 🏢 | 无障碍友好商场/医院 |

- 点击大图标 → 分散成小图标
- 点击小图标 → 查看详情图片

### 帮

1. 地图下方悬空「拍照按钮」，按下读取 GPS 坐标 + 上传照片
2. SpoonOS Agent 调用多模态大模型验证
3. 验证逻辑：
   - **新设施** → 添加到数据库 + 上链 + 发放全额 Token
   - **15 天内重复** → 拒绝（防刷单）
   - **15 天前更新** → 视为「维护数据」，发放 50% Token

---

## 上链信息

链上只存储：
- **Hash 值**（哈希指纹）
- **交易记录**

例如：`User A 在 Time T 验证了 Location X，获得 10 Tokens`

这保证了激励机制不可篡改。

---

## Tech Stack

| 层级 | 技术 |
|------|------|
| Frontend | Next.js 14, TypeScript, Tailwind CSS, RainbowKit, Leaflet |
| Backend | FastAPI, Python, Supabase (PostgreSQL + PostGIS) |
| Storage | Google Cloud Storage |
| AI | Gemini API (多模态验证) |
| Blockchain | Solidity, Sepolia Testnet, Alchemy RPC |

---

## 本地开发

### Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp env.example .env
# 编辑 .env 配置
uvicorn main:app --reload --port 8000
```

### Frontend

```bash
cd frontend
npm install
# 创建 .env.local
echo "NEXT_PUBLIC_API_URL=http://localhost:8000" > .env.local
echo "NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id" >> .env.local
npm run dev
```

---

## 项目结构

```
help2earn/
├── backend/
│   ├── main.py              # FastAPI 入口
│   ├── agent/               # SpoonOS Agent
│   ├── skills/              # Vision/Database/Blockchain/Storage
│   └── requirements.txt
├── frontend/
│   ├── src/app/             # Next.js 页面
│   ├── src/components/      # React 组件
│   └── package.json
├── contracts/
│   ├── contracts/           # Solidity 源码
│   └── scripts/             # 部署脚本
└── database/
    └── init.sql             # 数据库 schema
```

---

## License

MIT
